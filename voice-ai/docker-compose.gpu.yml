# Local GPU override is disabled because inference runs on FPT GPU.
# Uncomment this file only when intentionally running voice AI locally.
#
# services:
#   vieneu-tts:
#     build:
#       context: ./inference/vieneu-text-to-speech
#       args:
#         MIRROR_PREFIX: ${DOCKER_MIRROR:-nvidia}
#     deploy:
#       resources:
#         reservations:
#           devices:
#             - driver: nvidia
#               device_ids: ["0"]
#               capabilities: [gpu]
#     environment:
#       - TTS_DEVICE=${TTS_DEVICE:-cuda}
#       - TTS_GPU_MEM_FRACTION=${TTS_GPU_MEM_FRACTION:-0.65}
#       - PYTORCH_CUDA_ALLOC_CONF=max_split_size_mb:512,expandable_segments:True
#       - TORCH_ALLOW_TF32=1
#
#   whisper:
#     build:
#       context: ./inference/whisper
#       args:
#         WHISPER_BASE: ${WHISPER_BASE:-${DOCKER_MIRROR:-nvidia}/cuda:12.4.1-cudnn-runtime-ubuntu22.04}
#         MIRROR_PREFIX: ${DOCKER_MIRROR:-nvidia}
#     deploy:
#       resources:
#         reservations:
#           devices:
#             - driver: nvidia
#               device_ids: ["0"]
#               capabilities: [gpu]
#     environment:
#       - VOXBOX_DEVICE=${VOXBOX_DEVICE:-cuda}
#       - VOXBOX_COMPUTE_TYPE=${VOXBOX_COMPUTE_TYPE:-float16}

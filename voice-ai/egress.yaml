api_key: CHANGE_ME_LIVEKIT_API_KEY
api_secret: CHANGE_ME_LIVEKIT_API_SECRET
ws_url: ws://livekit:7880
redis:
  address: "redis:6379"

# S3 configuration can still be handled via environment variables inside Python start script
# But in egress, having the base YAML is sufficient.

services:
  vieneu-tts:
    build:
      context: ./inference/vieneu-text-to-speech
    ports:
      - "8298:8298"
    environment:
      - PORT=8298
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8298/health | grep -q '\"model_loaded\":true'"]
      interval: 10s
      timeout: 5s
      retries: 60
      start_period: 30s
    networks:
      - agent_network

  whisper:
    build:
      context: ./inference/whisper
    volumes:
      - whisper-data:/data
    environment:
      - VOXBOX_HF_REPO_ID=${VOXBOX_HF_REPO_ID:-Systran/faster-whisper-medium}
      - VOXBOX_DEVICE=cuda
      - DATA_DIR=/data
    ports:
      - "11437:8080"
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:8080/v1/models > /dev/null"]
      interval: 10s
      timeout: 5s
      retries: 150
      start_period: 30s
    networks:
      - agent_network
  
  livekit:
    image: livekit/livekit-server:latest
    command: --dev --config /etc/livekit.yaml
    volumes:
      - ./livekit-configs/livekit.yaml:/etc/livekit.yaml
    ports:
      - "7880:7880"
      - "7881:7881"
      - "7882:7882/udp"
      - "50000-50010:50000-50010/udp"
    networks:
      - agent_network

  livekit_agent:
    build:
      context: ./livekit_agent
    env_file:
      - ../.env
    volumes:
      - ./livekit_agent/src:/app/src
    environment:
      - LIVEKIT_URL=${LIVEKIT_URL:-ws://livekit:7880}
      - LIVEKIT_HOST=${LIVEKIT_URL:-ws://livekit:7880}
      - LIVEKIT_API_KEY=${LIVEKIT_API_KEY:-devkey}
      - LIVEKIT_API_SECRET=${LIVEKIT_API_SECRET:-secret}
      - LIVEKIT_AGENT_PORT=${LIVEKIT_AGENT_PORT:-7880}
      - STT_LANGUAGE=${STT_LANGUAGE:-vi}
      - LLM_BASE_URL=${LLM_BASE_URL:-http://host.docker.internal:11434/v1}
      - LLM_MODEL=${LLM_MODEL:-gemma4:e4b}
      - TTS_VOICE=${TTS_VOICE:-Bình (nam miền Bắc)}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-no-key-needed}
      - GROQ_API_KEY=${GROQ_API_KEY:-no-key-needed}
    depends_on:
      livekit:
        condition: service_started
      vieneu-tts:
        condition: service_healthy
      whisper:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8081/ > /dev/null"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 10s
    networks:
      - agent_network

  frontend:
    build:
      context: ../frontend
    ports:
      - "3005:3000"
    environment:
      - NEXT_PUBLIC_LIVEKIT_URL=ws://localhost:7880
      - LIVEKIT_URL=ws://livekit:7880
      - LIVEKIT_API_KEY=devkey
      - LIVEKIT_API_SECRET=secret
    depends_on:
      livekit:
        condition: service_started
    networks:
      - agent_network

volumes:
  whisper-data:

networks:
  agent_network:
    driver: bridge


